{
  "name": "@stefanasemota/sabi-auth",
  "version": "1.4.3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    "./client": {
      "types": "./dist/client.d.ts",
      "default": "./dist/client.js"
    },
    "./server": {
      "types": "./dist/server.d.ts",
      "default": "./dist/server.js"
    }
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@stefanasemota/sabi-logger": "git+https://github.com/stefanasemota/sabi-logger.git#v2.0.3",
    "@stefanasemota/stripe-core-service": "git+https://github.com/stefanasemota/stripe-core-service.git#v1.0.11",
    "firebase": ">=10.0.0",
    "server-only": "^0.0.1"
  },
  "scripts": {
    "test": "npx jest --coverage",
    "build": "tsc",
    "ship": "git push origin main --tags",
    "prepare": "npm run build && (test -d .git && npx husky install || true)",
    "release:patch": "npm version patch && git push origin main --tags",
    "release:minor": "npm version minor && git push origin main --tags",
    "release:major": "npm version major && git push origin main --tags",
    "postversion": "echo \"Successfully bumped to $npm_package_version\""
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.2",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "firebase-admin": "^13.6.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.2.0",
    "next": "^14.0.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "firebase": ">=10.0.0",
    "next": ">=13.0.0",
    "react": ">=18.0.0"
  }
}
